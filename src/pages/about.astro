---
import BaseLayout from '@/layouts/BaseLayout.astro'
import '@/styles/global.css'

const pageTitle = 'About';
---

<BaseLayout pageTitle={pageTitle}>
  <article class="about-container">
    <!-- Header -->
    <header class="about-hero">
      <div class="ambient-glow"></div>
      <h1 class="title">花木瑞 <span class="sub-title">/ huamurui</span></h1>
      <p class="tagline">A blog about web tech and nonsense.</p>
      <p class="poetic-intro">
        "痛苦如此持久，像蜗牛充满耐心地移动；快乐如此短暂，像兔子的尾巴掠过秋天的草原。"
      </p>
      <p class="poetic-intro2">
        关于 `礼仪` 与 `规则` 的讨论还有一大段，  </br>
        我现在还会想到一个线性代数公开课上的一句 `they're not rules,they're tools`。 </br>
        世界是物质的，粗粝的，不为意志所转移的，  </br>
        但这个世界上总有些东西，对你而言不是冷冰冰的必须服从的规则，  </br>
        而是发自内心的喜爱，与珍视和呵护。  </br>
      </p>
    </header>

    <!-- Main content - conversational style -->
    <div class="about-body">

      <section class="about-section">
        <p>
          这是一个编程和胡说八道混在一起的 blog。
        </p>
        <p>
          许多文章看起来很碎片化，嗯，它们确实挺碎片的，它们就是碎片。也许有时候我会尝试翻翻这堆东西，从里找出那么几段拼出一个还算连贯的东西，但，更多的时候，它们就是这样碎碎的。
        </p>
      </section>

      <div class="divider">· · ·</div>

      <section class="about-section">
        <p>
          除了写代码，还会做些别的。
          买过一些木头磨。
          买了个相机，想去拍一些好看的没见过的东西。
        </p>
        <p>
          里面的文章大概分三类：
        </p>
        <p>
          <span class="list-label">1.</span>
          <a href="/posts/nonsense/" class="preview-link">"nonsense"</a> 栏目里有一堆关于意识形态、精神分析、黑格尔、尼采、齐泽克的胡说八道，
          以及让 AI 扮演齐泽克用排泄物辩证法分析 Rust 的所有权系统之类的…嗯，东西。
        </p>
        <p>
          <span class="list-label">2.</span>
          <a href="/posts/tech/" class="preview-link">"tech"</a> 里是一些和编程相关的东西，其实之前有更多的，但都删掉，藏起来了。有些...笨拙，也没什么用了。倒是余下的笑话片段多少还有些有趣。
        </p>
        <p class="aside-block">
          也许那时所有东西都还很新奇，但现在看就是，像什么都不懂还爱絮絮叨叨的老妈妈。
          但还是想记下些东西，什么都好。
          一些，在我知道和不知道，熟悉和不熟悉之间的东西。
        </p>
        <p>
          <span class="list-label">3.</span>
          <a href="/posts/diary/" class="preview-link">"diary"</a> 就是纯粹的日记，碎片中的碎片。
        </p>
      </section>

      <div class="divider">· · ·</div>

      <section class="about-section">
        <p>
          这个博客的主题叫 <strong>Pudding</strong>, 是我自己糊的。
          名字来源大概是…小布丁？
          <span class="aside">好想做小布…小布尔乔亚啊…啊，不对，是小布尔什维克…小布丁。</span>
        </p>
      </section>

      <div class="divider">· · ·</div>

      <section class="about-section">
        <h2>在看在用的一些东西</h2>
        <div class="tags-container">
          <span class="tool-tag">Astro</span>
          <span class="tool-tag">Vue.js</span>
          <span class="tool-tag">TypeScript</span>
          <span class="tool-tag">React Native</span>
          <span class="tool-tag">Vite</span>
          <span class="tool-tag">WebGL / Shader</span>
          <span class="tool-tag">RAG</span>
          <span class="tool-tag">木头</span>
          <span class="tool-tag">砂纸</span>
          <span class="tool-tag">相机</span>
          <span class="tool-tag">齐泽克</span>
        </div>
      </section>

    </div>

    <!-- Footer -->
    <footer class="about-footer">
      <p>
        博客基于 <a href="https://github.com/huamurui/pudding" class="theme-link">Pudding</a> 主题构建。
      </p>
      <p class="copyright">© 2026 huamurui. Made with 🤍 and lots of daydreaming.</p>
    </footer>
  </article>

  <style>
    /* Container & Layout */
    .about-container {
      max-width: 720px;
      margin: 0 auto;
      padding: var(--spacing-xl) var(--spacing-md);
      display: flex;
      flex-direction: column;
      gap: var(--spacing-lg);
      position: relative;
    }

    /* Ambient Lighting Effect */
    .ambient-glow {
      position: absolute;
      top: -80px;
      left: 50%;
      transform: translateX(-50%);
      width: 350px;
      height: 200px;
      background: radial-gradient(circle, var(--color-theme-mix-20) 0%, transparent 70%);
      filter: blur(40px);
      z-index: -1;
      pointer-events: none;
      opacity: 0.6;
    }

    /* Hero */
    .about-hero {
      text-align: center;
      padding: var(--spacing-xl) 0 var(--spacing-sm);
      position: relative;
    }

    .about-hero .title {
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      font-weight: var(--font-weight-extrabold);
      color: var(--text-primary);
      margin: 0;
      letter-spacing: -0.02em;
    }

    .about-hero .sub-title {
      font-size: var(--font-size-lg);
      font-weight: var(--font-weight-normal);
      color: var(--text-muted);
      font-family: var(--font-family-mono);
    }

    .tagline {
      font-size: var(--font-size-sm);
      font-family: var(--font-family-mono);
      color: var(--text-muted);
      margin: var(--spacing-xs) 0 0;
      letter-spacing: 0.02em;
    }

    .poetic-intro {
      font-size: var(--font-size-base);
      font-style: italic;
      color: var(--text-secondary);
      max-width: 520px;
      margin: var(--spacing-md) auto 0;
      line-height: var(--line-height-relaxed);
    }

    .poetic-intro2 {
      font-size: var(--font-size-sm);
      font-style: italic;
      color: var(--text-secondary);
      max-width: 520px;
      margin: var(--spacing-md) auto 0;
      line-height: var(--line-height-relaxed);
      text-align: start;
    }

    /* Body */
    .about-body {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }

    .about-section {
      line-height: var(--line-height-relaxed);
      color: var(--text-secondary);
      font-size: var(--font-size-base);
    }

    .about-section p {
      margin: 0 0 var(--spacing-sm);
    }

    .about-section p:last-child {
      margin-bottom: 0;
    }

    .about-section h2 {
      font-size: var(--font-size-lg);
      font-weight: var(--font-weight-semibold);
      color: var(--text-primary);
      margin: 0 0 var(--spacing-sm);
    }

    .about-section strong {
      color: var(--text-primary);
      font-weight: var(--font-weight-semibold);
    }

    /* Inline aside — lighter, parenthetical */
    .aside {
      color: var(--text-muted);
      font-size: 0.92em;
    }

    /* Block aside — styled like a diary excerpt */
    .aside-block {
      color: var(--text-muted);
      font-size: var(--font-size-sm);
      border-left: 2px solid var(--border-color);
      padding-left: var(--spacing-md);
      margin-top: var(--spacing-sm);
      font-style: italic;
      line-height: var(--line-height-relaxed);
    }

    /* Divider */
    .divider {
      text-align: center;
      color: var(--text-muted);
      font-size: var(--font-size-sm);
      letter-spacing: 0.5em;
      padding: var(--spacing-xs) 0;
      user-select: none;
      opacity: 0.5;
    }

    /* List */
    .about-list {
      list-style: none;
      padding: 0;
      margin: var(--spacing-xs) 0 0;
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .about-list li {
      font-size: var(--font-size-sm);
      color: var(--text-secondary);
      padding: var(--spacing-xs) var(--spacing-sm);
      border-radius: var(--radius-md);
      background-color: var(--bg-secondary);
      border: 1px solid var(--border-color);
      transition: border-color var(--transition-normal);
    }

    .about-list li:hover {
      border-color: var(--color-theme-mix-20);
    }

    .list-label {
      font-family: var(--font-family-mono);
      font-weight: var(--font-weight-semibold);
      color: var(--theme-color);
      font-size: var(--font-size-sm);
    }

    /* Tags */
    .tags-container {
      display: flex;
      flex-wrap: wrap;
      gap: var(--spacing-sm);
    }

    .tool-tag {
      font-size: var(--font-size-sm);
      font-family: var(--font-family-mono);
      color: var(--text-secondary);
      background-color: var(--bg-secondary);
      border: 1px solid var(--border-color);
      padding: 0.35rem 0.7rem;
      border-radius: var(--radius-lg);
      transition:
        transform var(--transition-slow),
        border-color var(--transition-normal),
        color var(--transition-normal);
      cursor: default;
      user-select: none;
    }

    .tool-tag:hover {
      color: var(--theme-color);
      border-color: var(--theme-color);
      transform: translateY(-1px);
    }

    /* Footer */
    .about-footer {
      text-align: center;
      padding: var(--spacing-lg) 0;
      border-top: 1px solid var(--border-color);
      font-size: var(--font-size-sm);
      color: var(--text-muted);
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .about-footer p {
      margin: 0;
    }

    .theme-link {
      color: var(--theme-color);
      text-decoration: none;
      font-weight: var(--font-weight-semibold);
      border-bottom: 1px solid transparent;
      transition: border-color var(--transition-normal);
    }

    .theme-link:hover {
      border-bottom-color: var(--theme-color);
    }

    .copyright {
      font-size: var(--font-size-xs);
    }
  </style>
</BaseLayout>
